server:
  port: 8071

spring:
  application:
    name: config-server
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vl-blinov/spring-banking-system-services-config.git
          search-paths: /config
